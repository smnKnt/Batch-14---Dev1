string firstVaribale = 'This is our first string variable';
system.debug(firstVaribale);

string firstName = 'Osman';
string lastName = 'Kunt';
string fullName = firstName + ' ' + lastName;
system.debug('full name is ... ' + fullName);

string declareString; // declare a variable
system.debug(declareString);